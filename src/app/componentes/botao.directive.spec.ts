import { BotaoDirective } from './botao.directive';

describe('BotaoDirective', () => {
  it('should create an instance', () => {
    const directive = new BotaoDirective();
    expect(directive).toBeTruthy();
  });
});
